
Last tested auto mode that works: Our2Shoot5Bump
Ended with working on Shoot3Our3Shoot3 (not finished testing)

> Confirm that the shooter pre-spin bug is fixed
> Confirm that autonomous steps are now appended to the status strings
> Confirm that the end-of-match timer is fixed
> Check that the master robot mode controller is working as desired
> Check that the simulated IMU is working as desired (check sign)


> LIDAR tuning
> PDP port failure detection (claims issue on two PDP ports) (put a timer on it?)

> Drive rotation lock hysteresis (look for issues after module is fixed)

> Defensive swerve pivot point selection
> Turret code